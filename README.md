1️⃣ What is the difference between var, let, and const?
ans::=> var, let, const
var → old, function-scoped
let → block-scoped, changeable
const → block-scoped, fixed


2️⃣ What is the spread operator (...)? 
ans::=> Spread operator ...
Copy + expand array/object

3️⃣ What is the difference between map(), filter(), and forEach()?
ans::=> map(), filter(), forEach()
map() → transform → new array
filter() → select → new array
forEach() → loop → no return

4️⃣ What is an arrow function?
ans::=> Arrow function ()=>
Short function, return automatic if 1 line

5️⃣ What are template literals?
ans::=>Template literals ` `
Strings with ${variable}, multi-line easy
